import /data/zones/*

. {
    bind {$DNS_IP}
    health
    prometheus 0.0.0.0:9253
    errors
    reload 30s
    # log
    forward . tls://{$DNS_UPSTREAM1} tls://{$DNS_UPSTREAM2} {
      tls_servername {$DNS_UPSTREAM_NAME}
    }
    cache 30
}
